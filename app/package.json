{
  "name": "function-catalog-qry-svc",
  "version": "1.0.0",
  "description": "A simple endpoint with typescript",
  "main": "index.js",
  "author": "Krafox",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "lint": "eslint",
    "compile": "tsc",
    "watch": "esbuild src/functions/**/*.ts --bundle --minify --outdir=dist --outbase=src --sourcemap=inline --platform=node --target=node18.20.0 --watch"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.7",
    "esbuild": "^0.20.2",
    "esbuild-jest": "^0.5.0",
    "esbuild-node-tsc": "^2.0.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.1.0",
    "@middy/core": "^5.3.2"
  }
}
